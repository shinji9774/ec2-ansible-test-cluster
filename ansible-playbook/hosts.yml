all:
  vars:
    ansible_user: ubuntu
    #ansible_ssh_private_key_file: "{{ local_setup_private_key_file.dest }}"
    #home_key_pair_name: "{{ home_key_pair_name }}"
    #aws_region: "{{ aws_region }}"
  hosts:
    children:
      master:
        master-server:
          #ansible_host: "{{ target_master }}"
      targets:
        target01:
          #ansible_host: "{{ target_target01 }}"
        target02:
          #ansible_host: "{{ target_target02 }}"
